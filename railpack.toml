[build]
cmd = "rm -rf vendor && composer install --no-dev --no-scripts && npm install && npm run build && php artisan optimize && php artisan migrate --force && php artisan config:cache && php artisan route:cache"

