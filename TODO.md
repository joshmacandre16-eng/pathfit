1. [x] Edit Dockerfile: Replace the failing `RUN if [ -f \"package.json\" ]; then npm run build; fi` command since node_modules is pre-built and npm is not available in production stage.&#10;2. [ ] Test docker build success.&#10;3. [ ] Attempt completion.
